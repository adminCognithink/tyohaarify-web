# Environment Variables Configuration
# Copy this file to .env.local and fill in your actual API keys

# Google OAuth Configuration (for NextAuth.js)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_random_string

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (if using PostgreSQL instead of SQLite)
# DATABASE_URL="postgresql://username:password@localhost:5432/tyohaarify"

# Sharp Configuration (for image processing)
SHARP_CACHE_PATH=./tmp/sharp_cache

# Production Configuration
# Set NODE_ENV=production for production builds
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace placeholder values with your actual API keys
# 3. For Google OAuth: Create project at https://console.cloud.google.com/
# 4. For OpenAI API: Get key from https://platform.openai.com/api-keys
# 5. Generate NEXTAUTH_SECRET: openssl rand -base64 32